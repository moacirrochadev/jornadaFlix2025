package OrientacaoObjetos;

public class ClassesAbstratas {
    public static void main(String[] args) {
        /*
        CLASSES ABSTRATAS
        - Uma classe abstrata é uma classe que não pode ser instanciada diretamente, ou seja, não é possível
        criar objetos diretamente a partir dela.
        - É usada como uma classe base para fornecer um conjunto comum de características e comportamentos
        compartilhados por suas subclasses.
        - Um classe abstrata pode conter métodos concretos (com implementação) e métodos abstratos.
        Ex.: abstract class Animal{}
        - É marcado com a palavra-chave "abstract" e não contém um corpo (não tem código dentro das chaves);
        - Os métodos abstratos devem ser implementados nas subclasses que herdam da classe abstrata.
        - Métodos são usados para definir um contrato que as subclasses devem cumprir, fornecendo sua
        própria implementação.
        Ex.: abstract class Animal{
               public abstract void emitirSom();
              }
        * */
    }
}
